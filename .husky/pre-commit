#!/usr/bin/env sh

echo "Running pre-commit checks..."

cd backend || exit 1
go test ./... || exit 1

cd ../frontend || exit 1
bun run lint || exit 1
bun run typecheck || exit 1
bun run test || exit 1

echo "Pre-commit checks passed."
